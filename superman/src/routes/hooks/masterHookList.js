import { hooks as campaignHooks } from "../../models/campaign.js";
import { hooks as broadcastBrandTokenHooks } from "../../models/broadcastBrandToken.js";

export var allHooks = {
	campaign: campaignHooks,
	broadcastBrandToken: broadcastBrandTokenHooks,
    // todo-graham: need to fill in
};
